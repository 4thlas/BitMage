package bitmage.Utils;

import java.util.HashMap;
import bitmage.Enums.Option;

public class Command
{
    private Option mapTypeOption; // 8 or 16 char map
    private Option displayModeOption; // Normal or inverted display

    public Command(String rawCommand)
    {
        //mapTypeOption = getOptions(rawCommand).get("mapType");
        //displayModeOption = getOptions(rawCommand).get("displayMode");
    }

    //private HashMap<String, Option> getOptions(String rawCommand)
    //{

    //}
}
